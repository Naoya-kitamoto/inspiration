<main class="main">
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to image_tag("logo2.png", class: :logo), root_path %>
          </div>
<main class="main">
  <div class="inner">
    <div class="prototype__wrapper">
      <div class="prototype__body">
        <div class="prototype__detail">
          <p class="detail__title">アイデアのタイトル</p>
          <p class="detail__message">
            <%= @idea.title %>
          </p>
        </div>
      <%= link_to "by #{@idea.user.nickname}", root_path, class: :prototype__user %>
        <div class="prototype__detail">
          <p class="detail__title">アイデアの詳細</p>
          <p class="detail__message">
            <%= @idea.subject %>
          </p>
        </div>
          <% if @idea.image.attached? %>
      <div class="prototype__image">
        <%= image_tag @idea.image, class: "detail__image" %>
      </div>
    <% end %>
  </div>
      </div>
      <div class="prototype__comments">
        <%# ログインしているユーザーには以下のコメント投稿フォームを表示する %>
          <%# <%= form_with model: モデル名,local: true do |f|%>
            <div class="field">
              <%# <%= f.label :hoge, "コメント" %><br />
              <%# <%= f.text_field :hoge, id:"comment_content" %>
            </div>
            <div class="actions">
              <%# <%= f.submit "送信する", class: :form__btn  %>
            </div>
          <%# <% end %>
        <%# // ログインしているユーザーには上記を表示する %>
        <ul class="comments_lists">
          <%# 投稿に紐づくコメントを一覧する処理を記述する %>
            <li class="comments_list">
              <%# <%= " コメントのテキスト "%>
              <%# <%= link_to "（ ユーザー名 ）", root_path, class: :comment_user %>
            </li>
          <%# // 投稿に紐づくコメントを一覧する処理を記述する %>
        </ul>
      </div>
    </div>
  </div>
</main>
